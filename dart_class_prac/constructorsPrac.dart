// A constructors are special functions that run automatically once an instance of a class is created
// example

class Rectangle {
  double height;
  double width;
  Rectangle(this.height, this.width);
}
